import React from "react";

import CreateBookFormRender from "./render";

export const CreateBookFormComponent = (props: any) => (
  <CreateBookFormRender {...props} />
);

export default CreateBookFormComponent;
